// Demos for ShaderBackground. Each export is one usecase.
import ShaderBackground from "@/components/ui/shader-background";

const DemoOne = () => {
  return <ShaderBackground />;
};

export { DemoOne };
